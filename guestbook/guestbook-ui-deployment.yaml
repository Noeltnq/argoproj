apiVersion: apps/v1
kind: Deployment
metadata:
  name: guestbook-ui-trann
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app: guestbook-ui-trann
  template:
    metadata:
      labels:
        app: guestbook-ui-trann
    spec:
      containers:
      - image: gcr.io/heptio-images/ks-guestbook-demo:0.2
        name: guestbook-ui-trann
        ports:
        - containerPort: 80
